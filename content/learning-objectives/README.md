# Learning objectives

Store stable, versioned learning-objective identifiers here. IDs should be curriculum-provider-neutral where possible; jurisdiction-specific mappings can reference the stable internal objective.

Example internal ID: `math.measure.compare-lengths`.

Do not encode child names, demographic traits, diagnoses, school identifiers, or other personal data in objective IDs or mission definitions.
