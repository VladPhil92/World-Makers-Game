{
  "name": "@world-makers/runtime-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "World Makers runtime API gateway between game clients and the CTG One authenticated player-state API.",
  "scripts": {
    "start": "node src/server.mjs",
    "lint": "node scripts/lint.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run lint && npm test"
  },
  "engines": {
    "node": ">=22"
  }
}
