{
  "name": "@world-makers/player-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Player-facing World Makers hub for avatar, modes, worlds, store discovery and secure game launch.",
  "scripts": {
    "start": "node src/server.mjs",
    "lint": "node scripts/lint.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run lint && npm test"
  },
  "engines": {
    "node": ">=22"
  }
}
